package com.example.core.rule.ui.objects.space

import com.example.core.rule.ui.move.PlanetMoving

interface SpaceListener {

    fun changeSpace(space: Space)

//    fun changeFoodOnSpace(food: FoodOnSpace)

//    fun changePlanetMoving(planetMoving: PlanetMoving)
}